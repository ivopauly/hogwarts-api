# Hogwarts API

Project context for this repo lives in `AGENTS.md`, which is the vendor-neutral file
other agent tooling also reads. It is imported below so there is a single source of
truth and the two cannot drift apart.

**Edit `AGENTS.md`, not this file.**

@AGENTS.md
